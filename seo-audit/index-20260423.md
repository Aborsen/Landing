# SEO Audit — Insightis Landing Page (`index.html`)
_Generated: 2026-04-23 · Source: `D:\New Landing\index.html` · Page type: product / landing_

## TL;DR
- The page is a React SPA that mounts into `#root`, but the author shipped a **hidden SEO fallback block** (offscreen `aria-hidden` div at lines 1229–1280) containing a proper H1, TL;DR paragraph, FAQ, comparison table, and semantic outline. This is the *only* crawler-facing content — it is well-engineered and rescues the page from total SEO collapse.
- However, the fallback is **hidden with `aria-hidden="true"` + off-screen positioning**, which Google currently tolerates but is a **P0 risk**: it is indistinguishable from cloaking/hidden-text patterns. Recommend converting to server-rendered, *visible* HTML (or move it to visible below-the-fold copy).
- Meta, OG/Twitter, canonical, theme-color, and three JSON-LD blocks (SoftwareApplication, Organization, FAQPage) are all present and correctly formed.
- **Critical semantic gaps:** no `<main>`, no `<header>`, no `<article>`, zero `aria-label` attributes on any of the ~37 interactive `<button>` elements, and **zero `<img>` tags** (every visual is inline SVG, none with `<title>`/`<desc>`/`role="img"`/`aria-label`).
- Highest-impact next action: move the SEO fallback block out of the hidden off-screen container and into **server-rendered, visible** content, then add `aria-label`s to all icon-only buttons and social/nav links.

## Snippet preview (extension-style)
| Field | Value | Status |
|---|---|---|
| Title (chars) | `Insightis — AI Analytics Platform for Decision Intelligence` (60) | PASS |
| Description (chars) | `Insightis — AI-powered analytics workspace. Connect your data, ask questions in plain English, get instant insights with an AI Semantic Layer.` (143) | PASS |
| Keywords | missing | PASS (intentional; Google ignores) |
| URL | `https://insightis-landing.vercel.app/` | — |
| Canonical | `https://insightis-landing.vercel.app/` | WARN (vercel preview domain; prod canonical should be the final Devart/Insightis domain) |
| Robots Tag | `index,follow,max-image-preview:large` | WARN (author-noted TODO: switch Vercel previews to `noindex,nofollow`) |
| Author | missing | WARN |
| Publisher | `Devart` (via JSON-LD `publisher`) | PASS |
| Lang | `en` | PASS |
| Robots.txt | skipped (local file) | — |
| Sitemap.xml | skipped (local file) | — |
| Counts | H1: 2 (rendered) · H2: 8 · H3: 8 · H4: 5 · H5: 0 · H6: 0 · Images: 0 `<img>` · Links: ~45 | H1 count flagged below |

> **H1 caveat:** there is **1 H1 in the crawler-visible DOM** (line 1230, in the hidden-but-served fallback). React then renders a **second H1** (line 2957, "Your data knows the answer / Now you can ask it"). Once the React app hydrates, the DOM contains **two H1s** — that is a rule violation regardless of which one users see.

## Heading map

Crawler-visible (no-JS / pre-hydration) outline — from the hidden fallback block:
```
H1  Insightis — AI Analytics Platform for Decision Intelligence
├── H2  What Insightis does
│    ├── H3  Connect any data source
│    ├── H3  Auto-generated dashboards
│    └── H3  AI-written insight summaries
├── H2  How Insightis compares to traditional BI          (+ proper <table>)
├── H2  Who uses Insightis
│    ├── H3  Analysts
│    ├── H3  Product managers
│    └── H3  Executives
├── H2  Frequently asked questions
│    ├── H3  What is Insightis?
│    ├── H3  Which data sources does Insightis support?
│    └── H3  Do I need to know SQL to use Insightis?
└── H2  Start deciding with AI today
```

Post-hydration (rendered to users) outline — from React components:
```
H1  Your data knows the answer / Now you can ask it        ⚠ second H1 on page
H2  The Semantic Intelligence Layer
H2  Loved by a community
H2  What is Insightis
H2  Why teams switch to Insightis
     ├── H3  Traditional Approach
     └── H3  With Insightis
H2  Built for Decision Velocity                             (from <template id="showcase-template">)
H3  Start Making Smarter Decisions Today                    ⚠ orphan H3 used where H2 expected (SubscribeBanner CTA)
H3  Still waiting on insights that take days?              ⚠ orphan H3 used where H2 expected (BottomCTA)
H4 × 5  Platform / Solutions / Resources / Company / Legal  (footer column labels — acceptable but could be <h2>/<h3> under a footer H2)
```

Issues called out inline:
- `⚠ H1 count` — once React hydrates, there are two H1s on the page.
- `⚠ orphan H3` — two H3s (SubscribeBanner, BottomCTA) act as major section headlines but use H3, skipping the logical H2 tier.
- `⚠ duplicate semantics` — "What Insightis does" (fallback H2) and "What is Insightis" (rendered H2) carry overlapping intent but different copy; Google will pick the rendered one; good alignment is desirable.
- Rendered outline has **no FAQ H2** and **no pricing H2** (Pricing component uses "Why teams switch to Insightis"; the headline is a comparison, not a pricing block despite the component name).

## Scorecard
| Category | Status | Notes |
|---|---|---|
| Meta tags & snippet | PASS | Title 60 chars, desc 143 chars, OG + Twitter + theme-color complete |
| Heading hierarchy   | FAIL | 2 H1s post-hydration; 2 orphan H3s; fallback tree is clean, rendered tree is not |
| Grounding budget    | PASS (fallback) / WARN (rendered) | Fallback ~440 words. Rendered page is mostly marketing microcopy + JS demo — short-form, but no single H2 section exceeds 540 words |
| Content blocks 0–5  | PARTIAL | TL;DR exists in fallback (~95 words); missing in visible rendered page |
| Visuals & extraction| WARN | Comparison table only in fallback; rendered page uses two-column feature lists (not a `<table>`); zero `<img>` (all SVG) — fine for perf, but no alt/desc attributes |
| Linking strategy    | FAIL | No external authority outlinks (Microsoft Learn, Oracle Docs, Snowflake docs, etc.); only 3 Devart links and all point to `devart.com` root, not deep product/cluster pages |
| FAQ                 | PARTIAL | 3 FAQ items exist in fallback + JSON-LD; NOT in visible UI for users |
| Schema.org          | PASS | SoftwareApplication + Organization + FAQPage all present and valid |
| Semantic HTML & a11y| FAIL | Zero `<main>`, `<header>`, `<article>`; zero `aria-label` in a page with 37 buttons and dozens of icon-only links; mobile menu `<button>` has no accessible name |

## Findings (prioritized)

### [P0] Hidden off-screen SEO content pattern resembles cloaking
- **Current:** Lines 1229 and 1280:
  ```html
  <div style="position:absolute;left:-9999px;top:auto;width:1px;height:1px;overflow:hidden" aria-hidden="true">
    <h1>Insightis — AI Analytics Platform for Decision Intelligence</h1>
    ...
  </div>
  ```
- **Rule:** §2B Semantic HTML + general Google Webmaster guidelines on hidden text. AI crawlers (GPTBot, Google-Extended, Perplexity) increasingly devalue or skip hidden content. The content is load-bearing for every ranking signal on this page.
- **Recommendation:** Either (a) server-render the landing page so the Hero/features copy is in the initial HTML and delete the hidden block, or (b) move that exact copy into a **visible** section below the hero (rebrand it as "Overview" / "What is Insightis" / "FAQ"). Do not keep `aria-hidden="true"` + `left:-9999px` as the sole strategy.
- **Example fix:** Keep the fallback markup but swap the wrapper:
  ```html
  <main id="seo-content">
    <section class="container prose">
      <h1>Insightis — AI Analytics Platform for Decision Intelligence</h1>
      ...
    </section>
  </main>
  ```
  Then visually design it (not `display:none`, not `left:-9999px`).

### [P0] Two H1s on the hydrated DOM
- **Current:** H1 at line 1230 (`Insightis — AI Analytics Platform for Decision Intelligence`) plus React-rendered H1 at line 2957 (`Your data knows the answer / Now you can ask it`).
- **Rule:** §2C — exactly one H1 per page. ≥2 H1 is a P0.
- **Recommendation:** Demote the Hero's `<h1>` to the canonical form (match the title/meta), or keep the Hero H1 and delete the fallback H1 (assuming fallback becomes visible content per the finding above — in that case make its top heading an H2, e.g., "What Insightis is").
- **Example fix:** In `Hero()` (line 2957), change to:
  ```jsx
  <h1 className="ins-text-display-xl text-center mb-6">
    <div><span className="text-white">Insightis — AI Analytics for</span></div>
    <div><span style={{color:'var(--ins-text-highlight)'}}>Decision Intelligence</span></div>
  </h1>
  ```
  Keep the current "Your data knows the answer / Now you can ask it" line as a sibling `<p className="ins-text-body-lg">` tagline.

### [P0] Entire visible page is JS-rendered; initial HTML body contains only a loader
- **Current:** Line 1229 — `#root` initial HTML is a spinner + the hidden SEO block. Every section (Hero, Architecture, HowItWorks, Testimonials, Pricing, Footer) is rendered by React via `ReactDOM.createRoot(...).render(<App />)` at line 3901, which is compiled at runtime by `@babel/standalone`.
- **Rule:** §3 Step 1 — "page is JS-rendered; static HTML lacks crawlable content."
- **Recommendation:** Move to Next.js / Astro / any SSR or static-generation pipeline. Serve pre-rendered HTML for the crawler, hydrate for interactivity. `@babel/standalone` in production is also a performance P0 (≈1 MB, parses every visit). At minimum use a Vite/Rollup build and swap to precompiled JSX.
- **Example fix:** Migrate `App.jsx` into a Vite React + `vite-plugin-ssg` or Next.js `app/page.tsx`; generate `index.html` at build time with the full DOM in place.

### [P1] Orphan H3s used as section headlines
- **Current:** Line 3831 `<h3>Start Making Smarter Decisions Today</h3>` (SubscribeBanner) and line 3862 `<h3>Still waiting on insights that take days?</h3>` (BottomCTA) — both are top-of-section headlines but use H3.
- **Rule:** §2C — no orphan H3; H3 must nest under an H2.
- **Recommendation:** Promote both to `<h2>`; style is controlled by Tailwind classes, not tag.
- **Example fix:**
  ```jsx
  <h2 className="text-2xl md:text-3xl lg:text-4xl font-medium text-white mb-3 tracking-tight leading-tight">
    Start Making Smarter Decisions <span className="text-[var(--ins-color-teal-400)]">Today</span>
  </h2>
  ```

### [P1] No `<main>`, `<header>`, `<article>` — only `<section>`, `<nav>`, `<footer>`
- **Current:** `Grep` confirms zero occurrences of `<main>` or `<header>` in the file.
- **Rule:** §2B Semantic HTML.
- **Recommendation:** Wrap the App's content in `<main id="main-content">`; wrap the navigation pill + hero in `<header>`; use `<article>` for major content chunks (About, Features, FAQ).
- **Example fix:** In `App()` (line 3885):
  ```jsx
  function App() {
    return (
      <div className="font-body">
        <Header />
        <main id="main-content">
          <Hero />
          <Architecture />
          <HowItWorks />
          <SubscribeBanner />
          <Testimonials />
          <WhatIsInsightis />
          <Pricing />
          <BottomCTA />
        </main>
        <Footer />
      </div>
    );
  }
  ```

### [P1] Zero `aria-label` across 37 buttons and dozens of icon-only links
- **Current:** `Grep` for `aria-label` / `ariaLabel` returns 0 matches. Examples missing accessible names:
  - Line 2782 mobile menu toggle: `<button onClick={() => setMobileOpen(!mobileOpen)} className="md:hidden p-2">{mobileOpen ? <CloseIcon /> : <MenuIcon />}</button>`
  - Lines 3809–3812 social footer links — icon-only `<a>` with just an SVG, no accessible text.
  - The chat-mockup Attach / Connectors buttons (lines 3001, 3029) are icon-only.
- **Rule:** §2B Accessibility — "all interactive elements must have `aria-label` or clear descriptive text."
- **Recommendation:** Add `aria-label` to every button/link whose only child is an SVG.
- **Example fix:**
  ```jsx
  <button aria-label={mobileOpen ? 'Close menu' : 'Open menu'} onClick={...}>
  <a aria-label="Insightis on X (Twitter)" href="https://x.com/Insightisai" ...>
  <a aria-label="Insightis on LinkedIn" href="https://www.linkedin.com/company/112025589" ...>
  <button aria-label="Attach a file" ...>
  <button aria-label="Open connector picker" ...>
  ```

### [P1] No outbound authority links (Block "Linking strategy")
- **Current:** The only external links go to `devart.com/*` and social networks. No links to Microsoft Learn, Snowflake docs, BigQuery docs, Postgres docs, or any authority source — despite the page naming Snowflake, BigQuery, Redshift, PostgreSQL, MySQL, SQL Server, Oracle.
- **Rule:** §2A Linking strategy — "link to Microsoft Learn, Oracle Docs, etc. to validate claims."
- **Recommendation:** Add 2–3 external authority links in the (future visible) content section next to connector claims.
- **Example fix:** Inside the "Which data sources does Insightis support?" FAQ answer:
  ```html
  Insightis ships with native connectors for
  <a href="https://www.snowflake.com/en/data-cloud/platform/" rel="noopener">Snowflake</a>,
  <a href="https://cloud.google.com/bigquery/docs" rel="noopener">BigQuery</a>,
  <a href="https://learn.microsoft.com/en-us/sql/" rel="noopener">SQL Server</a>, …
  ```

### [P1] Insufficient Devart inlinks (<2 deep cluster links)
- **Current:** Three links to `https://www.devart.com/` root at lines 1275, 1277, 1279 — all the same URL, none to a deep cluster page.
- **Rule:** §2A Linking strategy — "2–3 related Devart articles using descriptive anchors."
- **Recommendation:** Replace the three identical devart.com root anchors with links to distinct Devart cluster pages (dbForge, ODBC drivers, Data Studio, etc.).
- **Example fix:**
  ```html
  Insightis is built by <a href="https://www.devart.com/company/">Devart</a>,
  the team behind <a href="https://www.devart.com/dbforge/">dbForge database tools</a>
  and <a href="https://www.devart.com/odbc/">Devart ODBC drivers</a>.
  ```

### [P1] Pricing component lacks actual pricing / no Pricing H2
- **Current:** The component `Pricing()` at line 3607 is actually a "Why teams switch" comparison. There is no tiered pricing, no "$X/mo" values, no `Product` or `Offer.priceSpecification` detail besides the placeholder `"price":"0"` in `SoftwareApplication` JSON-LD.
- **Rule:** §2C — "Pricing / FAQ / Conclusion present as their own H2 on product and tutorial pages." §2A — the `SoftwareApplication` schema's `offers.price: "0"` is misleading if actual plans are paid.
- **Recommendation:** Either add a real pricing section with tiered cards **or** remove the misleading `offers` block from JSON-LD. If Insightis is truly free, state so visibly.
- **Example fix:** Add a `<section id="pricing"><h2>Pricing</h2>…</section>` with at minimum a Free / Team / Enterprise card grid, and update the JSON-LD `offers` to an array of Offer objects.

### [P1] Vercel preview canonical URL and `index,follow` on preview
- **Current:** Line 8 `canonical = https://insightis-landing.vercel.app/` and line 9 author TODO: "on Vercel *preview* deployments, change to noindex,nofollow."
- **Rule:** §2A Meta tags & snippet — canonical should point to the production URL only.
- **Recommendation:** Introduce a build-time env guard. On preview builds emit `<meta name="robots" content="noindex,nofollow">` and drop `<link rel="canonical">`; on the production domain emit canonical to the final Insightis URL (e.g. `https://www.insightis.ai/` or `https://insightis.devart.com/`).

### [P1] FAQ exists in JSON-LD and in the hidden block, but is invisible to the user
- **Current:** `<script type="application/ld+json">` FAQPage at lines 1195–1226 declares three Q&A pairs, but the visible (React) UI has no FAQ section. Google's FAQ rich-result policy requires the Q&A to be visible to the user on the page.
- **Rule:** §2A FAQ section — "voice search & NLP; complete Q&A pairs; answers 40–60 words." Plus Google's structured-data policy: FAQPage content must also be visible.
- **Recommendation:** Add a visible FAQ `<section>` rendered by React containing the same three questions (and 3–5 more from "People Also Ask") with expand/collapse behavior.
- **Example fix:** Insert a new `FAQ()` component between `<Pricing />` and `<BottomCTA />`. Populate from the same source as the JSON-LD to keep them in sync.

### [P2] No `<img>` tags at all — every visual is inline SVG with no accessibility attributes
- **Current:** Grep returns zero `<img ` matches. The OG image exists on disk (`/assets/V_1_Dark_corrected.png`) but it is *only* used in the social preview meta tags, never in the page body.
- **Rule:** §2B Accessibility + §2A Visuals & extraction — "Diagrams with descriptive alt text (process described in detail, not 'image of…')."
- **Recommendation:** (a) Add `role="img"` + `aria-label="…"` on each decorative/meaningful SVG that conveys content (e.g., the Architecture diagram, connector grid, logo). (b) For purely decorative SVGs, add `aria-hidden="true"`. (c) Consider adding at least one meaningful `<img>` with a descriptive `alt` for the hero dashboard screenshot.
- **Example fix:**
  ```jsx
  <svg role="img" aria-labelledby="arch-diagram-title">
    <title id="arch-diagram-title">
      Insightis architecture: fragmented data sources on the left flow
      into the Semantic AI engine at the center, producing dashboards,
      reports, proactive signals, and collaborative analytics on the right.
    </title>
    …
  </svg>
  ```

### [P2] Comparison data exists, but the visible UI replaces the `<table>` with two columns of `<ul>`
- **Current:** Pricing component at line 3607 renders two parallel `<ul>` lists (Traditional vs Insightis). A proper HTML `<table>` with thead/tbody exists only in the hidden fallback at line 1242.
- **Rule:** §2A Visuals & extraction — "Comparison table (Feature | Native Method | Devart Tool). AI prioritizes tabular data."
- **Recommendation:** Render the "Traditional vs Insightis" comparison as an actual `<table>` in the visible page. Keep the styling; table elements can be styled with Tailwind.
- **Example fix:**
  ```jsx
  <table className="w-full comparison-grid">
    <thead><tr><th>Capability</th><th>Traditional</th><th>Insightis</th></tr></thead>
    <tbody>
      <tr><td>Data aggregation</td><td>Manual</td><td>Automated unification</td></tr>
      <tr><td>Query interface</td><td>SQL / Python</td><td>Natural language</td></tr>
      <tr><td>Report setup</td><td>Days</td><td>Instant, zero code</td></tr>
      <tr><td>Tooling</td><td>Siloed, fragmented</td><td>Unified intelligence layer</td></tr>
      <tr><td>Engineering overhead</td><td>High</td><td>10× more cost-effective</td></tr>
    </tbody>
  </table>
  ```

### [P2] Performance & crawlability: runtime Babel, Tailwind CDN, and React UMD
- **Current:** Lines 35, 61–63:
  ```html
  <script src="https://cdn.tailwindcss.com"></script>
  <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
  <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
  <script crossorigin src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
  ```
  Plus `framer-motion` full UMD (~130 kB) at line 64.
- **Rule:** §2B implicit — Core Web Vitals drive rankings; Babel-in-browser blocks first paint.
- **Recommendation:** Switch to a Vite/Next build; ship compiled JSX + Tailwind JIT CSS; remove `@babel/standalone`. Self-host fonts (already preloaded locally — good). Add a `<link rel="preload" as="image">` for the LCP asset once the hero screenshot is real.

### [P2] Footer column labels use `<h4>` under no parent `<h2>`/`<h3>`
- **Current:** Lines 3742, 3752, 3762, 3772, 3789 — five `<h4>` tags for column labels ("Platform", "Solutions", "Resources", "Company", "Legal") directly inside `<footer>` with no enclosing heading hierarchy.
- **Rule:** §2C — no skipped levels.
- **Recommendation:** Wrap the footer columns in a visually-hidden `<h2>Site map</h2>` and demote the column labels to `<h3>` — or keep them as `<h4>` but add a preceding `<h3 className="sr-only">Sitemap</h3>`. Either preserves the intended visual design.

### [P2] TL;DR block is only present in the hidden fallback
- **Current:** ~95-word Problem → Devart product (Insightis) → Benefit paragraph at line 1231 (hidden). The visible hero is a single sentence plus a chat demo — no TL;DR equivalent.
- **Rule:** §2A Block 0 — "TL;DR / Executive Summary (mandatory): 80–100 words, Problem → Solution (Devart Product) → Key Benefit."
- **Recommendation:** Add a visible 80–100-word paragraph directly below the hero H1, summarizing the problem / the Insightis solution / the benefit. The hidden fallback copy (reused almost verbatim) would satisfy the rule.

### [P2] No fan-out H3 sub-questions in the rendered UI
- **Current:** Block 3 per guideline is "Fan-out topic with 2+ H3 sub-questions." The visible page has zero question-phrased H3s. The fallback FAQ covers three questions but is hidden.
- **Rule:** §2A Block 3 Fan-out topic.
- **Recommendation:** In the new visible FAQ component, structure around a main H2 question ("How does Insightis turn data into decisions?") with 3+ H3 sub-questions derived from "People Also Ask": *What is an AI semantic layer? How is Insightis different from ChatGPT + CSV? Does Insightis replace my BI tool? Is it SOC 2 compliant?*

### [P2] `copyright` year in the footer
- **Current:** Line 3807: `&copy; Copyright © Devart 2026` — literally a double "Copyright ©" and already using 2026. Harmless but looks sloppy.
- **Rule:** N/A — polish.
- **Recommendation:** Replace with a dynamic year and single copyright mark: `{`${new Date().getFullYear()}`} Devart`.

## Suggested rewrites

- **`<title>`** (keep current — 60 chars, PASS): `Insightis — AI Analytics Platform for Decision Intelligence`
- **Alternative `<title>` with clearer format word**: `Insightis: AI Analytics Platform for Decision Intelligence` (58 chars) or `Insightis — AI Analytics Workspace for Decision Teams` (54 chars, includes format word "Workspace")
- **`<meta name="description">`** (keep current — 143 chars, PASS). Alt draft (152 chars, slightly stronger CTR verb): `Insightis is an AI analytics platform. Connect your real data, ask questions in plain English, and get accurate insights via the AI Semantic Layer.`
- **Visible `<h1>`** (replaces the dual-H1 situation): `Insightis — AI Analytics for Decision Intelligence` — with the poetic "Your data knows the answer / Now you can ask it" demoted to a tagline `<p>`.
- **URL slug**: `/` is fine for the root landing. When this grows into a full site, keep this page at `/` and move feature pages to `/platform/ai-chat/`, `/platform/semantic-layer/`, etc. (already implied by the footer).
- **Visible TL;DR block (80–100 words, ≈92 words)**:
  > Most teams drown in dashboards and still miss the decisions that matter. Traditional BI answers questions only after an analyst writes SQL, and the insight lands days late. **Insightis** is an AI analytics platform from Devart that connects to your existing data sources, auto-builds the dashboards your team actually needs, and writes plain-English insight summaries the moment anomalies appear. The result: decisions in minutes, not sprints — without hiring a larger analytics team, rebuilding pipelines, or migrating your warehouse.

- **FAQ entries (top 3 fan-out questions, each 40–60 words)**:
  1. **Q: What is an AI Semantic Layer?** A: An AI Semantic Layer is a governed model that defines what every metric, dimension, and business term in your data actually means. Insightis builds one automatically from your connected sources so that every AI-generated answer uses the same definitions your analytics team already trusts. *(49 words)*
  2. **Q: How is Insightis different from pasting data into ChatGPT?** A: ChatGPT can reason about a CSV you paste, but it has no live connection to your warehouse, no governance, and no memory of your metric definitions. Insightis connects directly to Snowflake, BigQuery, Postgres, and 100+ other sources, validates every generated SQL query, and keeps definitions consistent across every dashboard it produces. *(53 words)*
  3. **Q: Does Insightis replace my existing BI tool?** A: No. Insightis sits alongside tools like Looker, Tableau, or Power BI and augments them with natural-language queries and AI-written insight summaries. Teams typically keep their existing dashboards and use Insightis to investigate anomalies, prototype new metrics, and generate narrative reports — without waiting on an analyst. *(48 words)*

- **JSON-LD stubs to add**:
  ```json
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://www.insightis.ai/" }
    ]
  }
  ```
  ```json
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "Insightis",
    "url": "https://www.insightis.ai/",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "https://www.insightis.ai/search?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  }
  ```
  Also tighten the existing `SoftwareApplication` block: replace the placeholder `offers.price: "0"` with either a correct `PriceSpecification` array or remove `offers` entirely until pricing is public.

## Auto-applicable vs. editorial judgment

**An optimizer agent can safely auto-apply (low risk, no copy-writing):**
- Add `aria-label` to every icon-only `<button>` and `<a>` (inferring the label from surrounding `svg`/tooltip state — the existing `setTooltip('attach')` / `setTooltip('connectors')` identifiers are perfect sources).
- Wrap `App()`'s children in `<main id="main-content">` and rename the outer Hero wrapper to `<header>`.
- Promote the two orphan `<h3>` tags (SubscribeBanner, BottomCTA) to `<h2>` — Tailwind classes unchanged.
- Add `role="img"` + `aria-hidden="true"` (for decorative) or `aria-label` (for meaningful) to every inline SVG that currently has neither.
- Inject the `WebSite` and `BreadcrumbList` JSON-LD blocks into `<head>`.
- Change footer `<h4>`s to `<h3>`s and insert a visually-hidden `<h2 class="sr-only">Site map</h2>`.
- Replace the static copyright year string with a React expression.
- Add `rel="noopener"` to any external link still missing it (spot-check found most already have `rel="noopener noreferrer"`).
- Emit `<meta name="robots" content="noindex,nofollow">` on preview builds via an env check.

**Requires editorial judgment (ask a human):**
- Whether to keep the dual-H1 pattern or rewrite the Hero headline. The current "Your data knows the answer / Now you can ask it" is marketing voice; replacing it with a keyword-rich H1 is a brand decision.
- Whether the hidden SEO fallback block should become a visible section, and where on the page it should live.
- Whether to migrate to SSR/SSG (Next.js / Astro). This is a large architectural change.
- Selection of deep-link targets on `devart.com` (which cluster pages are canonical today).
- Accuracy of the comparison claims (10×, 80%, 1,200+ companies, 99.9% uptime, "3× more accurate") — these must be fact-checked before publication and cannot be auto-asserted.
- Pricing: actual tiers and what the JSON-LD `offers` should declare.
- The three fan-out FAQ questions above are good candidates, but the answers (especially the ChatGPT comparison) need legal/marketing sign-off.
- Final canonical domain (`insightis.ai`? `insightis.devart.com`? something else).
- Whether to add a real hero dashboard image (`<img>` with descriptive `alt`) and, if so, which screenshot.

## Out of scope / questions for the author
- Confirm the production domain (Vercel preview vs. final Devart/Insightis URL) so canonical, OG URL, and JSON-LD `url` can be finalized.
- Confirm the primary keyword. Current meta implies "AI Analytics Platform" + "Decision Intelligence" — is that the intended primary?
- Confirm target audience. Footer persona list suggests RevOps, Founders, Marketing, Product, Data, Ops & Finance — that's six; which is the primary for this root landing?
- Is Insightis a standalone paid product, a Devart add-on, or a freemium offering? This drives Pricing section + JSON-LD `offers` accuracy.
- Are the three fallback-FAQ answers approved marketing copy, or placeholders to be rewritten?
- robots.txt and sitemap.xml were not checked (local file input, no origin to fetch). Before go-live, verify that `/robots.txt` allows crawling and `/sitemap.xml` exists and lists this URL.
